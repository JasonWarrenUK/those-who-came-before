/**
 * Role advancement
 * doc 07, section 4.2
 *
 * Phase 21 implementation target.
 */

import type { AcademicRole, CareerState } from '$lib/types/career.js';

export function checkPromotion(_careerState: CareerState): AcademicRole | null {
	throw new Error('Not implemented — Phase 21');
}
