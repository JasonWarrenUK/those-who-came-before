/**
 * Slow-burn strain accumulation
 * doc 06, section 5
 *
 * Phase 17 implementation target.
 */

import type { HypothesisStrain } from '$lib/types/interpretation.js';

export function accumulateStrain(
	_hypothesisId: string,
	_currentStrain: HypothesisStrain
): HypothesisStrain {
	throw new Error('Not implemented — Phase 17');
}
