/**
 * Form classification (emergent from properties)
 * doc 10
 *
 * Phase 16 implementation target.
 */

import type { DocumentNode } from '$lib/types/documents.js';

export function classifyForm(_document: DocumentNode): string {
	throw new Error('Not implemented — Phase 16');
}
