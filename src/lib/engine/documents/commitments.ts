/**
 * Commitment extraction and tracking
 * doc 10
 *
 * Phase 16 implementation target.
 */

import type { Commitment } from '$lib/types/documents.js';

export function extractCommitments(_content: string): Commitment[] {
	throw new Error('Not implemented — Phase 16');
}
