/**
 * Venue generation, prestige computation, editorial process
 * doc 07, section 3.1
 *
 * Phase 20 implementation target.
 */

import type { VenueDefinition } from '$lib/types/venues.js';

export function generateVenues(_prng: () => number): VenueDefinition[] {
	throw new Error('Not implemented — Phase 20');
}
