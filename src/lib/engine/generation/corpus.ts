/**
 * Initial corpus generation (NPC scholars, documents, dating)
 * doc 05, section 4
 *
 * Phase 10 implementation target.
 */

import type { ProfessionalCorpus, WorldState } from '$lib/types/world.js';

export function generateCorpus(
	_worldState: WorldState,
	_prng: () => number
): ProfessionalCorpus {
	throw new Error('Not implemented — Phase 10');
}
