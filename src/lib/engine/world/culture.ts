/**
 * Culture generation
 * doc 05, section 3
 *
 * Phase 8 implementation target.
 */

import type { Culture } from '$lib/types/world.js';

export function generateCulture(_prng: () => number): Culture {
	throw new Error('Not implemented — Phase 8');
}
