/**
 * Scholar entity generation (player + NPC)
 * doc 07, section 5
 *
 * Phase 10 implementation target.
 */

import type { MinimalScholar } from '$lib/types/scholars.js';

export function generateScholar(_prng: () => number): MinimalScholar {
	throw new Error('Not implemented — Phase 10');
}
