/**
 * Period generation
 * doc 05, section 3
 *
 * Phase 8 implementation target.
 */

import type { WorldChronology } from '$lib/types/world.js';

export function generateChronology(_prng: () => number): WorldChronology {
	throw new Error('Not implemented — Phase 8');
}
